import FlowBlock from './FlowBlock';

export default function validator(flowBlock: FlowBlock) {
    // const manifest = flowBlock.getManifest();
    // // Check if all parameters of the block have valid names.
    // // Remember to update the error message as well when changing the regex.
    // const paramNamesMustMatch = /^[A-Z0-9_]+$/;
    // const paramWithInvalidName = manifest.parameters.find(p => !paramNamesMustMatch.test(p.name));
    // if(paramWithInvalidName) {
    //     return `Invalid name for parameter '${paramWithInvalidName.name}'. Parameter names can only contain uppercase A-Z, 0-9 and underscore.`;
    // }
}
